import 'package:flutter/material.dart';

class ex extends StatelessWidget {
  const ex({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    body:Card(child: Image.asset('assets/images/dreamsville house.png'),),
    );
  }
}
